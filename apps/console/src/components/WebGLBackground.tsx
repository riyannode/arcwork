'use client';

import DotMatrixField from './DotMatrixField';

/**
 * Unified ambient background for all console routes.
 * Mirrors landing — dot-matrix gold field with scanline sweep.
 */
export default function WebGLBackground() {
  return <DotMatrixField />;
}

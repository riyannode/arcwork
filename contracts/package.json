{
  "name": "arclayer-contracts",
  "version": "1.0.0",
  "description": "ArcLayer smart contracts for Arc Network",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:testnet": "forge script script/DeployArcLayer.s.sol --rpc-url https://rpc.testnet.arc.network --broadcast",
    "verify": "forge verify-contract"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
